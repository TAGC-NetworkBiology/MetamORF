
The scripts of the current folder allow to download the cross references 
(Gene lists) from Ensembl databases using biomaRt (R) for H. sapiens and
M. musculus.

The "DefaultOutputFolder.txt" file should contain one single line with the
path to the output folder where to store the cross-references and the log 
file.
